1. RCSDoorLockUnlockService 소스 코드 분석

    1.1 - RCSDoorLockUnlockService.hpp

              RCSDoorLockUnlockServiceBase 상속받음

              가상함수 bool sendHTTPRequest (mStatus, mReason)

              ?동적 적재? RCSDoorLockUnlockService_Init()

    1.2 - RCSDoorLockUnlockService.cpp

              생성자 RCSDoorLockUnlockService
              mServiceName = "RCSDoorLockUnlockService"
              ServiceBase의 addCenterEventArrived(
                URI_RC_DOOR_UNLOCK, [=](
                  const std::string &strUri,
                  const std::string &strOutJsonHeader,
                  const std::string &strOutJsonBody
                  ){rdoCenterEventArrivedHandle(
                      strUri,
                      strOutJsonHeader,
                      strOutJsonBody
                    );
                  }
              )
              이와 같이 Lock도 생성시에 넣는다.

              bool sendHTTPRequest (mStatus, mReason)
              "RCSDoorLockUnlockService_sendHTTPRequest_KOREA+++++++++++++++++++ Tid=%s", remoteDoorTid.c_str(), 0,0
              로그에 위 문장을 찍고
              json데이터와 status, reason, statustemp 등 값을 선언
              mstatus가 statuscode.h 의 204, no content 가 아니라면
              RemoteData의 getInstance().setCountryInfo(KOREA);를 하고
              InJasonData는 MakeJson의 getInstance().makeVehicleStatus(mStatus);를 해준다
              그외에 InJsonData = ""; 초기화
              MsgLow로
              sendHTTPRequest Tid=%s", remoteDoorTid.c_str(), 0,0
              sendHTTPRequest Uri=%s", remoteDoorUri.c_str(), 0,0
              sendHTTPRequest m_Host=%s", m_Host, 0,0
              sendHTTPRequest m_Action=%d",m_Action, 0,0 찍어주고
              string Host = (string)m_Host;
              ?모르는 부분?
              만약 받은 값을 정합성 검사해서 맞으면
              MsgLow로
              "HTTP Send : /response",0,0,0 해주고 true 반환

              ?동적 적재? 관련 함수

    1.3 - RCSDoorLockUnlockServiceBase.hpp

    1.4 - RCSDoorLockUnlockServiceBase.cpp

2. 윈도우 AD join, 보안 프로그램 설치, 기본 툴 설치(진행 중)

3. Void 포인터, 함수형 포인 숙지

4. C++ FILE fin, fout 숙지

5. 파일 암, 복호화 코드 리뷰

6. extern "C" 오버로딩 안하는 C언어형 함수 숙지

7. 가상함수 virtual 개념 숙지
