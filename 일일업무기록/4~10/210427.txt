1. 2.8 빌드

    jw1 2.8 빌드 완료

    ce1 2.8 teleapp 빌드 에러 보고

    #if !defined (TARGET_CCU_CE) //DataBuffer 에서 처럼 실행되지 않게 막아버리면 해결 가능

    왠만하면 상황 파악 후 차종에 맞게

    #elif defined (CARTYPE_JW1)
    #endif

    이런식으로 직관성 있게 막을 것


2. 목요일 정도에 김석환책임님께 메일

    안녕하십니까!
    인포뱅크 IVI 개발 3팀 조기성입니다.

    Static, Dynamic Architecture 수정 중 문의 사항이 있어 메일 드립니다.
    vehicle data, gps data 2612 다이어그램 수정 여부
    purpose가 내부 컴포넌트라면 외부 컴포넌트 테이블 따로 작성 여부
    2815 참고하여 다이어그램 제작 여부

3. 외국에서 ProvStatus 상태 2, 3일 시 gps 데이터 막는 코드 머지, beyond compare를 통한 변경점 확인

    타프로젝트 소스가 정의되어 있어
    ce빌드에서 제외되도록 적용
    ce 빌드

    -TeleApp  머지 : TAG:
    git add .
    git commit -m "[CCU AP][CCS App][CE1][CCUG1-6247] 해외 향지 가개통 상태 서버 메시지 전송 시 센터 프로토콜 GPS 값 미전송 이슈 수정
          - 이슈 내용 : 해외 향지에서 PRE-RDR(BASIC/SHELL) 상태로 센터 통신 시 센터 프로토콜 헤더에 정의된 GPS 정보가 전송되는 이슈 발생
          - 원인 / 대책 : 해외 향지 검사 및 PRE-RDR(BASIC/SHELL) 상태 검사 후 GPS 정보를 상황에 맞게 전달하는 분기문의 부재 / 조건문을 통해 국내, 해외 식별 및 PRE-RDR(BASIC/SHELL) 상태 검사 후 상황에 맞는 GPS 정보 송신
          - 변경점 : TeleManager 코드
          - 영향도 : CCU ALL
          - 테스트 방법 : 가개통 상태 센터 통신 필요"
    git push ccu HEAD:DEV-CCUG1-6247-DT074224


    [CCU AP][CCS App][CE1] 빌드 에러 수정 적용
    자체수정
    [현상]
    CCNF 2.8 브랜치에서 CE1 빌드 시 에러 발생
    [원인]
    타 차종 시그널 포함으로 인한 빌드 에러
    [대책]
    CE1 프로젝트에서 타 차종 시그널 제외 적용
    [테스트 방법]
    CE1 빌드

4. 사무용품 구매 링크

    2.1 랜 to usb 케이블 http://www.officeplus.com/prod/item/item.jsp?itemCode=D68728

    2.2 다이어리 http://www.officeplus.com/prod/item/item.jsp?itemCode=EF4113

    2.3 모니터 받침 http://www.officeplus.com/prod/item/item.jsp?itemCode=FT6997

    2.4 라벨링 http://www.officeplus.com/prod/item/item.jsp?itemCode=AN6792

5. 차종에 따른 전처리기 사용

    ce1이 차종에 추가됨에 따라 ce1을 상단에 분기하여 처리한다.

    //ex)
    #if defined (TARGET_CCU_CE)
    //default
    #elif defined (CARTYPE_RS4)
    //default
    #elif defined (CARTYPE_JW1)
    //default
    #endif

    #if defined (TARGET_CCU_CE)
    return HResult::OK;
    #else
    #if defined (CARTYPE_RS4)
    HUInt64 value=0;dsfa
    HResult ret = mp_Ethcoummunicator->getCCSignal(ccnf::vehicle::raw::HEthCCSignalID::IBU_RrStRmndAlrmSta, value);
    state = static_cast<HUInt16>(value);
    MSG_HIGH ("ret, getRrStRmndAlrmSta : %d %d", static_cast<int>(ret),state);
    return ret;
    #elif defined (CARTYPE_JW1)
    #if (CCNF_VER_MAJ == 2) && (CCNF_VER_MIN >=8)
    HUInt64 value=0;
    HResult ret = mp_Ethcoummunicator->getCCSignal(ccnf::vehicle::raw::HEthCCSignalID::IBU_RrStRmndAlrmSta, value);
    state = static_cast<HUInt16>(value);
    MSG_HIGH ("ret, JW1_getRrStRmndAlrmSta : %d %d", static_cast<int>(ret),state);
    return ret;
    #else
    return HResult::OK;
    #endif
    #endif
    #endif
